#include <bits/stdc++.h>

#define sep ' '
#define ll long long

using namespace std;

int howmany(int n){
    // HOW MANY TO FIND N IN RANGE (1, N+1)
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;
    cin >> n;

    cout << howmany(n);

    return 0;
}